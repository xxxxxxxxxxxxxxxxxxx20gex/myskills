interface:
  display_name: "Local Credential Memory"
  short_description: "Discover, import, and securely reuse local credentials"
  default_prompt: "Use $local-credential-memory to securely discover or import credentials from an authorized local configuration, then save, find, update, delete, or use them."
